## Running Locally

Always use the local server to view your pages:

```bash
python3 -m http.server 8000
```

Then visit: `http://localhost:8000`

Direct file:// URLs will not work due to CORS restrictions.
